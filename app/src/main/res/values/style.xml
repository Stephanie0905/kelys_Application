<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">#00000000</item>


        <item name="android:windowContentTransitions">true</item>


    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Menu Style -->
    <style name="navigationTheme">
        <!-- Color of text and icon when selected -->
        <item name="colorButtonNormal">#FF009688</item>
        <!-- Background color when selected -->
        <item name="colorControlHighlight">#E6FFC107</item>

    </style>

    <style name="MyDatePickerDialogTheme" parent="Theme.MaterialComponents.Light.Dialog.MinWidth">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:buttonBarPositiveButtonStyle">@color/colorDatePicker</item>
        <item name="android:buttonBarNegativeButtonStyle">@color/colorDatePicker</item>
    </style>


    <!-- PopUp Style -->
    <style name="AppTheme.PopUp">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>

    </style>

</resources>